app:
    datasource:
        url: jdbc:mysql://localhost:3306/hotelaria
        username: root
        password: P@$$w0rd!
        driver-class-name: com.mysql.cj.jdbc.Driver

spring:
  jpa:
    hibernate:
      ddl-auto : update
      show-sql: true
      properties:
        hibernate:
          format_sql: true
